# ch01 — Git 실습 자료

`practice_survey.csv`를 프로젝트 `data-raw/`에 복사해 1장 실습에 쓴다.

```bash
shasum -a 256 -c SHA256SUMS
```

책 1.5.3 본문 수치(남성 143/여성 157)가 이 파일에 의존한다. **교체하지 말 것.**
예상 시간: 60–90분(Git 기초 포함).
